apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rendszam-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: kulacs-beadando.jcloud.jedlik.cloud
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend-service
                port:
                  number: 3000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80
